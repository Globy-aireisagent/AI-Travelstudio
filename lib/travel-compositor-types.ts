/**
 * Barrel file that simply re-exports the full, auto-generated
 * type definitions for the Travel Compositor API.
 *
 * The real (very large) types live in
 *   lib/generated/travel-compositor-types.ts
 * so that we keep the root lib/ folder tidy.
 */

export * from "./generated/travel-compositor-types"
